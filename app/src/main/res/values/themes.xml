<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LoginApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/kobiche</item>
        <item name="colorPrimaryVariant">@color/ecru</item>
        <item name="colorOnPrimary">@color/lightorange</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ecru</item>
        <item name="colorSecondaryVariant">@color/ecru</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/lightorange</item>
    </style>

    <style name="Theme.LoginApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.LoginApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.LoginApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>